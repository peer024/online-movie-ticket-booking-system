# Frontend Environment Variables
# Leave empty for unified single-port deployment (uses same origin /api)
# Set to custom URL only if backend is hosted on a separate domain (e.g. https://api.yourdomain.com/api)
VITE_API_URL=
